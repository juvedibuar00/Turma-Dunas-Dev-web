// Seleciona o PRIMEIRO elemento que corresponde
const container = document.querySelector('#container');
const primeiroTexto = document.querySelector('.texto');

// Seleciona TODOS os elementos que correspondem (retorna NodeList)
const todosTextos = document.querySelectorAll('.texto');
const todosParagrafos = document.querySelectorAll('p');